# SEO Setup Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a full SEO setup to all pages — title template, OG/Twitter metadata on the landing page, `noindex` on all private/auth pages, `robots.txt`, and `sitemap.xml`.

**Architecture:** Uses Next.js App Router's native Metadata API exclusively. Root layout gets `metadataBase`, a title template, and OG/Twitter tags that all pages inherit. Each page overrides only what differs. Two new files (`robots.ts`, `sitemap.ts`) handle crawling rules and sitemap generation.

**Tech Stack:** Next.js App Router Metadata API (`next/dist` types: `Metadata`, `MetadataRoute`)

---

## Files Changed

| Action | File                                             |
| ------ | ------------------------------------------------ |
| Modify | `src/app/layout.tsx`                             |
| Create | `src/app/robots.ts`                              |
| Create | `src/app/sitemap.ts`                             |
| Modify | `src/app/(auth)/sign-in/[[...sign-in]]/page.tsx` |
| Modify | `src/app/(auth)/sign-up/[[...sign-up]]/page.tsx` |
| Modify | `src/app/onboarding/page.tsx`                    |
| Modify | `src/app/(app)/dashboard/page.tsx`               |
| Modify | `src/app/(app)/conversations/page.tsx`           |
| Modify | `src/app/(app)/leads/page.tsx`                   |
| Modify | `src/app/(app)/appointments/page.tsx`            |
| Modify | `src/app/(app)/leads/[leadId]/page.tsx`          |
| Modify | `src/app/(app)/settings/page.tsx`                |
| Modify | `src/app/(app)/settings/billing/page.tsx`        |
| Modify | `src/app/(app)/settings/workspace/page.tsx`      |
| Modify | `src/app/(app)/domains/[domainId]/page.tsx`      |
| Modify | `src/app/(chatbot)/chatbot/[domainId]/page.tsx`  |
| Modify | `src/app/(portal)/portal/[token]/page.tsx`       |

---

## Task 1: Root layout — full metadata with OG + Twitter

**Files:**

- Modify: `src/app/layout.tsx`

- [ ] **Step 1: Replace the metadata export in `src/app/layout.tsx`**

Replace the entire file with:

```tsx
import type { Metadata } from 'next'
import type { ReactElement, ReactNode } from 'react'

import { ClerkProvider } from '@clerk/nextjs'
import { Geist_Mono, Outfit } from 'next/font/google'

import '@/env'

import './globals.css'

const fontSans = Outfit({
  subsets: ['latin'],
  variable: '--font-sans',
})

const fontMono = Geist_Mono({
  subsets: ['latin'],
  variable: '--font-geist-mono',
})

export const metadata: Metadata = {
  metadataBase: new URL('https://www.sendora.forum'),
  title: {
    default: 'Sendora',
    template: '%s | Sendora',
  },
  description: 'AI-powered conversational sales and email marketing platform.',
  openGraph: {
    type: 'website',
    siteName: 'Sendora',
    title: 'Sendora',
    description:
      'AI-powered conversational sales and email marketing platform.',
    url: 'https://www.sendora.forum',
    images: [
      {
        url: '/images/app-ui.png',
        width: 1200,
        height: 630,
        alt: 'Sendora — AI-powered sales platform',
      },
    ],
  },
  twitter: {
    card: 'summary_large_image',
    title: 'Sendora',
    description:
      'AI-powered conversational sales and email marketing platform.',
    images: ['/images/app-ui.png'],
  },
}

const RootLayout = ({ children }: { children: ReactNode }): ReactElement => (
  <ClerkProvider
    appearance={{
      options: {
        unsafe_disableDevelopmentModeWarnings: true,
      },
    }}
  >
    <html
      className={[
        fontSans.variable,
        fontMono.variable,
        'h-full antialiased w-full',
      ]
        .filter(Boolean)
        .join(' ')}
      lang="en"
    >
      <body className="flex min-h-full flex-col w-full">{children}</body>
    </html>
  </ClerkProvider>
)

export default RootLayout
```

---

## Task 2: robots.ts and sitemap.ts

**Files:**

- Create: `src/app/robots.ts`
- Create: `src/app/sitemap.ts`

- [ ] **Step 1: Create `src/app/robots.ts`**

```ts
import type { MetadataRoute } from 'next'

export default function robots(): MetadataRoute.Robots {
  return {
    rules: [
      {
        userAgent: '*',
        allow: '/',
        disallow: [
          '/dashboard',
          '/conversations',
          '/leads',
          '/appointments',
          '/settings',
          '/onboarding',
          '/chatbot',
          '/portal',
          '/api',
        ],
      },
    ],
    sitemap: 'https://www.sendora.forum/sitemap.xml',
  }
}
```

- [ ] **Step 2: Create `src/app/sitemap.ts`**

```ts
import type { MetadataRoute } from 'next'

export default function sitemap(): MetadataRoute.Sitemap {
  return [
    {
      url: 'https://www.sendora.forum',
      lastModified: new Date(),
      changeFrequency: 'weekly',
      priority: 1,
    },
  ]
}
```

---

## Task 3: Auth and onboarding pages

**Files:**

- Modify: `src/app/(auth)/sign-in/[[...sign-in]]/page.tsx`
- Modify: `src/app/(auth)/sign-up/[[...sign-up]]/page.tsx`
- Modify: `src/app/onboarding/page.tsx`

- [ ] **Step 1: Update `src/app/(auth)/sign-in/[[...sign-in]]/page.tsx`**

```tsx
import type { Metadata } from 'next'
import type { ReactElement } from 'react'

import { SignIn } from '@clerk/nextjs'

export const metadata: Metadata = {
  title: 'Sign In',
  robots: { index: false, follow: false },
}

const SignInPage = (): ReactElement => <SignIn />

export default SignInPage
```

- [ ] **Step 2: Update `src/app/(auth)/sign-up/[[...sign-up]]/page.tsx`**

```tsx
import type { Metadata } from 'next'
import type { ReactElement } from 'react'

import { SignUp } from '@clerk/nextjs'

export const metadata: Metadata = {
  title: 'Sign Up',
  robots: { index: false, follow: false },
}

const SignUpPage = (): ReactElement => <SignUp />

export default SignUpPage
```

- [ ] **Step 3: Update `src/app/onboarding/page.tsx`**

```tsx
import type { Metadata } from 'next'
import type { ReactElement } from 'react'

import { currentUser } from '@clerk/nextjs/server'
import { OnboardingPage } from '@features/onboarding/components/onboarding-page'
import { redirect } from 'next/navigation'

import { listActivePlans } from '@/features/commercial/repositories/plan-repository'

export const dynamic = 'force-dynamic'

export const metadata: Metadata = {
  title: 'Onboarding',
  robots: { index: false, follow: false },
}

const Onboarding = async (): Promise<ReactElement> => {
  const clerkUser = await currentUser()

  if (!clerkUser) {
    redirect('/sign-in')
  }

  const plans = await listActivePlans()

  return (
    <main className="flex min-h-screen items-center justify-center bg-background">
      <OnboardingPage
        clerkFirstName={clerkUser.firstName}
        clerkLastName={clerkUser.lastName}
        plans={plans}
      />
    </main>
  )
}

export default Onboarding
```

---

## Task 4: Core app pages — dashboard, conversations, leads, appointments

Each gets `noindex` + a human title that feeds into the root layout template (e.g. `Dashboard | Sendora`).

**Files:**

- Modify: `src/app/(app)/dashboard/page.tsx`
- Modify: `src/app/(app)/conversations/page.tsx`
- Modify: `src/app/(app)/leads/page.tsx`
- Modify: `src/app/(app)/appointments/page.tsx`

- [ ] **Step 1: Add metadata to `src/app/(app)/dashboard/page.tsx`**

Add these two lines after the last `import` statement and before `const Dashboard`:

```ts
export const metadata: Metadata = {
  title: 'Dashboard',
  robots: { index: false },
}
```

Also add `Metadata` to the existing `next` import. The import line becomes:

```ts
import type { Metadata } from 'next'
```

(Add as a new import at the top of the file alongside the existing imports.)

- [ ] **Step 2: Add metadata to `src/app/(app)/conversations/page.tsx`**

Add at the top of the file (new import + export before the component):

```ts
import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Conversations',
  robots: { index: false },
}
```

- [ ] **Step 3: Add metadata to `src/app/(app)/leads/page.tsx`**

```ts
import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Leads',
  robots: { index: false },
}
```

- [ ] **Step 4: Add metadata to `src/app/(app)/appointments/page.tsx`**

```ts
import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Appointments',
  robots: { index: false },
}
```

---

## Task 5: Settings, lead detail, and domain pages

**Files:**

- Modify: `src/app/(app)/settings/page.tsx`
- Modify: `src/app/(app)/settings/billing/page.tsx`
- Modify: `src/app/(app)/settings/workspace/page.tsx`
- Modify: `src/app/(app)/leads/[leadId]/page.tsx`
- Modify: `src/app/(app)/domains/[domainId]/page.tsx`

- [ ] **Step 1: Add metadata to `src/app/(app)/settings/page.tsx`**

```ts
import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Settings',
  robots: { index: false },
}
```

- [ ] **Step 2: Add metadata to `src/app/(app)/settings/billing/page.tsx`**

```ts
import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Billing',
  robots: { index: false },
}
```

- [ ] **Step 3: Add metadata to `src/app/(app)/settings/workspace/page.tsx`**

```ts
import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Workspace Settings',
  robots: { index: false },
}
```

- [ ] **Step 4: Add metadata to `src/app/(app)/leads/[leadId]/page.tsx`**

```ts
import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Lead Detail',
  robots: { index: false },
}
```

- [ ] **Step 5: Add metadata to `src/app/(app)/domains/[domainId]/page.tsx`**

```ts
import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Domain Settings',
  robots: { index: false },
}
```

---

## Task 6: Chatbot widget and portal pages

These are public-facing embeds accessed by token/domainId — they must never appear in search results.

**Files:**

- Modify: `src/app/(chatbot)/chatbot/[domainId]/page.tsx`
- Modify: `src/app/(portal)/portal/[token]/page.tsx`

- [ ] **Step 1: Add metadata to `src/app/(chatbot)/chatbot/[domainId]/page.tsx`**

Add after the existing imports, before `export const dynamic`:

```ts
import type { Metadata } from 'next'

export const metadata: Metadata = {
  robots: { index: false, follow: false },
}
```

- [ ] **Step 2: Add metadata to `src/app/(portal)/portal/[token]/page.tsx`**

Add after the existing imports, before the component:

```ts
import type { Metadata } from 'next'

export const metadata: Metadata = {
  robots: { index: false, follow: false },
}
```

---

## Task 7: Build verification

- [ ] **Step 1: Run the build**

```bash
bun run build
```

Expected: build completes with no TypeScript errors. You should see `robots.txt` and `sitemap.xml` listed in the output as static routes.

- [ ] **Step 2: Spot-check rendered HTML**

Start the production server:

```bash
bun run start
```

Open `http://localhost:3000` and inspect the `<head>` in DevTools. Verify:

- `<title>Sendora</title>`
- `<meta property="og:title" content="Sendora">`
- `<meta property="og:image" content="https://www.sendora.forum/images/app-ui.png">`
- `<meta name="twitter:card" content="summary_large_image">`

Open `http://localhost:3000/robots.txt` and verify it lists the disallow rules and sitemap URL.

Open `http://localhost:3000/sitemap.xml` and verify it contains `https://www.sendora.forum`.

Open `http://localhost:3000/dashboard` and verify `<meta name="robots" content="noindex">` is present.
